@helper Submit(string value, string classe)
{
    <input type="submit" class="@classe" value="@value" />
}

@helper Mensagem(string mensagem, string classe)
{
    if (@mensagem != null)
    {
        <div class="@classe">
           @mensagem
        </div>
    }
}